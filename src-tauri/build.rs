/// Entry point for the crate's build script that runs the Tauri build process.
///
/// # Examples
///
/// ```no_run
/// // src-tauri/build.rs
/// tauri_build::build();
/// ```
fn main() {
    tauri_build::build()
}
